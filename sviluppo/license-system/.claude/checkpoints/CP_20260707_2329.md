# CP_20260707_2329 — AdOff Admin Panel Fix

## Goal
Admin panel `adoff.app/admin` funzionante con stats e analytics real-time.

## ✅ Done (verificati)

| ID | Cosa | Evidence |
|----|------|----------|
| D1 | Admin panel `adoff.app/admin` serve 101659 byte (Google login + full dashboard) | `curl -s https://adoff.app/admin \| wc -c` → `101659` |
| D2 | `/admin/stats` ritorna dati install tracking | `curl -H "Authorization: Bearer $ADMIN_TOKEN" https://api.adoff.app/admin/stats` → `{installs:0, licenses:0}` |
| D3 | `/admin/licenses` ritorna licenze | `curl -H "Authorization: Bearer $ADMIN_TOKEN" https://api.adoff.app/admin/licenses` → `{total:0, active:0}` |
| D4 | `/trial/check` server-authoritative (anti-cheat) | `curl "https://api.adoff.app/trial/check?deviceId=test123"` → `{active:true, daysLeft:30, serverAuthoritative:true}` |
| D5 | Admin panel committato + pushato | `master 0a33de6` |

## ⬜ To-Do (in ordine)

- [ ] **REAL-DATA** — Nessun install reale tracciato ancora: le tabelle D1 sono vuote (install_events=0, uninstall_events=0, device_heartbeat=0). Il sistema è deployato ma nessuna estensione in campo comunica ancora (serve publish nuova versione store).
      Comando: verificare con `curl -s -H "Authorization: Bearer $ADMIN_TOKEN" https://api.adoff.app/admin/stats`
      Done when: `installs.total` > 0

- [ ] **PUBLISH NUOVA VERSIONE STORE** — L'estensione pubblicata sugli store (CWS/AMO/Edge) è la 3.5.x e non ha ancora il codice install-tracking. Serve pubblicare v3.4.x+ con background.js che chiama `trackInstall()`/`trackHeartbeat()`/`trackUninstall()`.
      Comando: build + upload a CWS, AMO, Edge (vedi CLAUDE.md sezione Publish)
      Done when: nuova versione pubblicata su CWS + AMO + Edge

- [ ] **STORE CANCEL** — L'utente aveva chiesto link per annullare pubblicazioni pendenti (CWS 3.5.6 pending review). Non ancora risolto.

## ❌ Failed Approaches

- Merge agent (API error mid-response) → abbandonato merge retention, ripristinato admin working direttamente
- Deploy admin.html via `wrangler kv` locale → scriveva in KV locale, non remoto → fix con CF API REST diretta

## Do NOT

- Non toccare `sviluppo/license-system/worker.js` senza verificare `curl https://api.adoff.app/trial/check?deviceId=test`
- Le 3 cartelle `app/` `app-firefox/` `app-safari/` devono restare sincronizzate (CLAUDE.md Multi-Browser Sync rule)
- Admin panel HTML NON va modificato a mano: è gestito via KV upload da `admin.html` locale

## Key Decisions

- Install tracking: backend in worker.js (D1 + KV), frontend in background.js (chrome.alarms/heartbeat ogni 60 min)
- Trial anti-cheat: ECDSA P-256 signed token da `/trial/check`, server-authoritative, nessun fallback locale
- Admin panel: servito da KV via worker route `/admin`, non da Pages

## Current State

**HEAD**: `master 0a33de6` · Tutti gli endpoint funzionano, dati ancora a zero (nessun install reale in campo)

## Setup

```bash
cd "/mnt/backup/Dropbox/1 Programmazione/Progetti/ChromePlugin"
source ~/.secrets/adoff-stores.env
ADMIN_TOKEN="799f0e3ee8ad4dd24f1fd1aaa31fc640068b9c740f4a24cb"
```

## Warnings

- Stats/license a 0 è normale finché nessuna estensione in campo ha il codice install-tracking (background.js updated ma non ancora pubblicato sugli store)
- Trial token funziona: `device_heartbeat` per dispositivi già trial-active; `trials` D1 per install nuove

## Bootstrap prossima chat

```
Leggi il checkpoint: /mnt/backup/Dropbox/1 Programmazione/Progetti/ChromePlugin/sviluppo/license-system/.claude/checkpoints/CP_20260707_2329.md
Task corrente: pubblicare nuova versione AdOff sugli store (CWS + AMO + Edge) con il codice install-tracking attivo
Non toccare: worker.js (già funzionante), admin.html (già ripristinato)
Quando hai finito, committa con messaggio conventional, pusha, e fai /wiki all.
```

# AdOff — Experiment Playbook (pattern vincenti)

> Libreria dei pattern provati. Append automatico dal workflow `experiment-engine.workflow.json` al chiudersi di ogni esperimento vincente (skill ab-testing §Playbook).
> I pattern qui validati diventano la **linea brand provata** → l'automazione (`content-factory`) ne eredita i default via `hook-bank.json`. Le costanti narrative (`STORY-BIBLE-2026.md`) NON sono mai modificate da qui.

Formato entry (auto): `## <exp_id> — winner <A|B> (med save+share A=.. B=..) <data>`. Annotare a mano sotto: variabile, perché ha funzionato, pattern riusabile, dove applicarlo.

---

<!-- le entry vengono appese sotto questa riga dal workflow n8n -->

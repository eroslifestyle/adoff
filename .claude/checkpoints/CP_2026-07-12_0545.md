# CHECKPOINT — AdOff Autofix Dashboard Bugfix
**Data**: 2026-07-12 05:45 | **Commit**: `caec61c` | **Branch**: master

## Goal
Fix autofix dashboard UI rendering bug + restore automation flow (24 leaks stuck unfixed)

---

## Progress / TODO Board

### ✅ Done (verificati)

- [x] **FIX-UI-001** — `escHtml()` undefined in admin.html → ReferenceError in `renderLeakCard()` → panel stuck on "Carico i leak..."
      Fix: `replace_all escHtml → esc` in admin.html (14 occorrenze). KV `admin:html` aggiornato via CF API.
      Evidence: `grep -c escHtml admin.html → 0`, KV verificato con GET.
      Commit: `caec61c`

- [x] **FIX-AUTO-001** — `analyze_and_fix.mjs` stripped `candidate_rule` dal `tracked[]` → D1 riceveva null → auto_decide non poteva applicare
      Fix: `tracked` ora include `candidate_rule: c.rule`. Commit: prossimo.
      Evidence: state.json mostrava 24 leaks con `rule_id=null`.

- [x] **FIX-AUTO-002** — `apply_decisions.mjs` parsing API sbagliato + path rules-feed.json errato
      Fix: `SITE_BASE = ../../site`, bucket parsing da `data.buckets.tracking` (non `data.leaks`).
      Evidence: dry-run ora mostra 8 regole applicabili.

- [x] **DATA-RESTORE** — Re-ingestiti 24 leak in D1 con `candidate_rule` (via `/admin/autofix/ingest`)
      19 network_leak con regole DNR, 5 dom_visible_ad senza regola.
      8 regole ads network aggiunte a `rules-feed.json` (+8 → 37 totali).

- [x] **DEPLOY** — `rules-feed.json` (37 regole) deployato su Pages.
      CDN purged: full zone cache purge.

---

## ⬜ To-Do (in ordine)

- [ ] **VERIFY-002** — Verifica che `adoff.app/rules-feed.json` abbia 37 regole (cache CDN potrebbe ancora mostrare 28)
      Comando: `curl -s https://adoff.app/rules-feed.json | python3 -c "import sys,json; d=json.load(sys.stdin); print(len(d['rules']))"`
      Done when: output `37`

- [ ] **VERIFY-003** — Verifica che UI admin mostri i 24 leak con i bottoni "Fixa"
      Comando: apri `adoff.app/admin` → tab Autofix → verifica che le card leak abbiano i bottoni ✅/🚫/⏳
      Done when: almeno un leak mostra "Fixa" button non-disabilitato

- [ ] **FIX-CANARY** — Canary runner (`canary_runner.mjs`) non gira mai. Serve per validare le regole prima dell'applicazione.
      Comando: `cd sviluppo/autofix && node canary_runner.mjs --dry-run` (verifica che funzioni)
      Done when: output mostra canary validation per almeno una regola

- [ ] **MARK-APPLIED** — Dopo fix verificato: segnare le 8 regole come `applied=1` in D1
      Comando: `curl -X POST https://adoff.app/admin/autofix/decision -H "X-Admin-Token: $ADMIN_TOKEN" -d '{"applied":1,"fingerprints":[...]}'`
      Done when: D1 query mostra `applied=1` per gli 8 fingerprint

---

## ❌ Failed Approaches (non riprovare)

- **`replace_all escHtml` in tutto admin.html** — Non ha rotto nulla perché `esc()` è la funzione corretta usata nel resto del file. Solo `escHtml` era undefined.

- **Re-run `analyze_and_fix`** — Sovrascrive `candidate_rules/2026-07-10.json` perdendo le regole già calcolate. Il file va solo esteso, non ricalcolato.

- **Wrangler OAuth token refresh** — Scaduto e non refreshabile (403 Forbidden). Usa invece `CF_API_KEY` con `X-Auth-Email` per KV push.

---

## Do NOT

- Non toccare credentiali/secrets in `~/.secrets/adoff-stores.env`
- Non re-ingestire i 24 leak (già fatto, ripetere crea duplicati)
- Non usare `adoff-license-api.workers.dev` (NON esiste più — il worker è su `adoff.app/api/*`)

---

## Key Decisions

1. **Analytics/tracking leaks = non-fix**: googletagmanager, google-analytics, scorecardresearch, beacon → skip dal rules-feed (low confidence, non ads visibili all'utente). Solo googlesyndication/doubleclick/amazon-adsystem sono ads reali.

2. **Candidate rules reconstruction**: Quando `candidate_rules/*.json` è perso, rigenerare da `findings/*.json` (blocked_urls[] + domain) usando `generateUrlFilter()` pattern `||host^`.

3. **KV admin:html via Python urllib**: Wrangler OAuth scaduto; push via CF REST API con `X-Auth-Email` + `X-Auth-Key` headers.

---

## Current State

**HEAD**: `caec61c` (esc fix) — 3 precedenti commits autofix
**Worker**: LIVE su `adoff.app/api/*` (responde a `/admin/autofix/leaks`)
**KV admin:html**: aggiornato con fix `escHtml→esc`
**rules-feed.json**: 37 regole su Pages (`cfbbaaee.adoff-site.pages.dev`)
**D1**: 24 leaks con candidate_rule (19 network, 5 dom)
**UI**: fix `escHtml` in KV, ma cache CDN potrebbe servire la versione vecchia

---

## File Changes

- `sviluppo/license-system/admin.html` — `escHtml` → `esc` (14 fix, commit `caec61c`)
- `sviluppo/autofix/analyze_and_fix.mjs` — `tracked` include `candidate_rule` (PENDING COMMIT)
- `sviluppo/autofix/apply_decisions.mjs` — SITE_BASE fix + bucket parsing (PENDING COMMIT)
- `sviluppo/autofix/push_dashboard.mjs` — unchanged (logica già corretta)
- `site/rules-feed.json` — +8 rules, deployed (PENDING COMMIT)
- `sviluppo/autofix/candidate_rules/2026-07-10.json` — RISCRITTO dal re-run (19 candidate rules persi, ricostruiti da findings + re-ingest D1)

---

## Setup

```bash
cd /mnt/backup/Dropbox/1\ Programmazione/Progetti/ChromePlugin
source ~/.secrets/adoff-stores.env
```

---

## Warnings

- `candidate_rules/2026-07-10.json` è stato sovrascritto dal re-run di `analyze_and_fix` — le regole originali sono state ricostruite da `findings/2026-07-10.json` e re-ingestate in D1
- CF CDN cache per `adoff.app/rules-feed.json` potrebbe mostrare 28 regole (vecchia versione) per i prossimi 5 minuti — dopo si allinea a 37
- Wrangler OAuth token scaduto — usa `CF_API_KEY` per operazioni KV

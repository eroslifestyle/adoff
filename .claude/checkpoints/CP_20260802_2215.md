# Checkpoint — CP_20260802_2215 — generatore rule-feed bonificato, gate i18n sanato, 3.5.59 su tutti gli store — 2026-08-02

**Status:** Versione 3.5.59 pubblicata su Chrome, Firefox, Edge. Safari in attesa. Feed 2026.08.02.2 attivo e verificato end-to-end. Branch `feat/premium-vpn` HEAD `06dd73e` pushato.

## Goal

Correggere alla fonte il generatore del rule-feed remoto, che emetteva sintassi regex dentro `condition.urlFilter` rendendo inerte ogni feed prodotto; poi pubblicare la release sui store.

## TODO Board

### ✅ Done (verificato)

- Diagnosi: `convertPattern()` in `sviluppo/filter-lists/parse-adblock-to-dnr.js` costruiva pattern regex e sostituiva tutti i caratteri circonflesso compresi quelli interni; emetteva la chiave `trigger` invece di `condition`. Misurato sul feed sorgente: **35.000 regole regex su 35.143**, solo 136 valide.
- Nuovo modulo `sviluppo/filter-lists/abp-urlfilter.js`: punto unico di conversione e validazione, un dominio diventa `||dominio^`. `isValidUrlFilter` è il gate che rifiuta parentesi, backslash e schemi `http:`.
- `parse-adblock-to-dnr.js` riscritto: formato DNR con chiave `condition`, doppio gate per regola, blocco dell'output se meno del 50% dei pattern è ancorato al dominio. Risultato **132.300 regole, 97,1% ancorate**.
- Nuovo `sviluppo/filter-lists/rule-priority.js`: selezione a quote decisa dall'utente. Le regole `allow` entrano sempre tutte (eccezioni anti falso positivo), poi 60% del budget alle liste curate e 40% alla continuità col feed live. Selezione reale: **ALLOW 2/2, CURATED 17.399/25.737, LIVE 11.599/28.957, BULK 0/77.604**.
- Nuovo `sviluppo/filter-lists/build-rules-feed.js`: costruisce il feed e si rifiuta di scrivere se oltre il 5% degli `urlFilter` non è valido.
- Nuovo `sviluppo/tests/verify-generated-feed.js`: applica il feed in Chrome a blocchi di 2000 e interroga `testMatchOutcome`. Esito **29.000/29.000 applicate in 15 blocchi senza errori**, `0019x.com` bloccato dalla regola `id 177`, `primevideo/wikipedia/github` in `main_frame` liberi.
- Nuovo `sviluppo/tests/canary-feed-falsepositives.js`: canary sui falsi positivi. Su 8 siti navigati nessun sospetto attribuibile alle regole nuove.
- Il gate i18n eseguito da `deploy-site.sh` ora **passa**: da 774 hard failures a 0. Sanati i dati, non lo script: 29 chiavi usate nell'HTML ma assenti dalla matrice, 1.139 celle tradotte in 14 lingue, 7 chiavi `about.*` morte rimosse (zero riferimenti, valori corrotti). Nuovo `sviluppo/scripts/i18n_collect_gaps.py`.
- Feed `2026.08.02.2` pubblicato e verificato end-to-end sul percorso reale: `adoffRemoteRulesCount` uguale a **29000 in 3,0 secondi**, `0019x.com` bloccato.
- Store: Chrome Web Store **3.5.59** pubblicata (upload 200 `uploadState SUCCESS`, publish 200 status OK), Firefox AMO **3.5.59** public, Microsoft Edge **3.5.59** pubblicata (submission `1152921505701564400`). L'errore Edge era `401 API Key is Invalid`: chiave scaduta, rigenerata dall'utente e aggiornata in `~/.secrets/adoff-stores.env`.
- Commit sul branch `feat/premium-vpn`: `5da1a0a`, `b916ff6`, `0af3795`, `cd6e011`, `68dd62b`, `0934b4f`, `aad5ee0`, `06dd73e`. HEAD `06dd73e` pushato.

### ⬜ To-Do (in ordine, il prossimo in cima)

1. **Safari: 3.5.59 non pubblicata**, richiede Mac con Xcode e `xcrun safari-web-extension-converter`. Expected outcome: pacchetto firmato caricato su App Store Connect.
2. **Restano 1.561 celle untranslated** (warning soft, non bloccano il deploy), di cui 616 in italiano. Expected outcome: ridurle almeno per le lingue principali, riusando `sviluppo/scripts/i18n_collect_gaps.py` e la traduzione a blocchi da 25.
3. **Le 103.300 regole escluse dal cap**: oggi la quota è 60/40 fra curate e continuità. Expected outcome: valutare sui dati se quel rapporto va spostato.

## Failed Approaches

- **Verificare un feed senza isolare la rete**: il service worker scarica il feed live da `adoff.app`, satura il cap dinamico e i match arrivano da quello. Nel primo giro `0019x.com` risultava bloccato dalla regola `id 60143` del feed live, non dal candidato. Serve l'argomento Chrome `host-resolver-rules` che mappa `adoff.app` su `127.0.0.1`.
- **Confrontare feed vecchio e nuovo con stringhe grezze**: il feed storico scrive senza circonflesso finale, il generatore lo aggiunge. Senza chiave canonica il riconoscimento della continuità crolla da **28.957 regole a 5**.
- **Attribuire un blocco a una regola leggendo `result.matchResults[0]`**: `testMatchOutcome` restituisce `matchedRules` direttamente, e va filtrato per `rulesetId == "_dynamic"`, altrimenti gli id delle regole statiche vengono scambiati per regole del feed.
- **Tradurre 85 stringhe in una sola chiamata**: il modello restituisce JSON malformato o risposta vuota. Blocchi da 25 con validazione e retry.
- **Usare `GET /v1/products/ID/submissions` per testare l'autenticazione Edge**: quel GET non esiste e risponde sempre 404. L'unica prova valida è il POST.

## Do NOT

- **MAI** passare l'intero feed a una sola `updateDynamicRules`: è atomica, fallisce in blocco e l'errore non emerge.
- **MAI** usare `MAX_NUMBER_OF_DYNAMIC_AND_SESSION_RULES` (5.000) come cap: il valore giusto è `MAX_NUMBER_OF_DYNAMIC_RULES` (30.000), letto a runtime.
- **NON** considerare "feed arrivato" un fetch riuscito: l'unico indicatore affidabile è `adoffRemoteRulesCount` in storage.
- **NON** pubblicare `site/rules-feed.json` prodotto a mano: passa dalla pipeline `parse-adblock-to-dnr.js` → `build-rules-feed.js`. `sviluppo/scripts/normalize-rules-feed.js` resta solo come difesa di riserva.
- **NON** usare `wrangler pages deploy` diretto: bypassa il gate i18n. Serve `sviluppo/scripts/deploy-site.sh`, che passa già branch `main`.
- **MAI** merge di `feat/premium-vpn` su `main`: `main` è il repo pubblico open-core.
- **NON** reintrodurre `video.currentTime` in `activateYoutubeRuntimeKiller`.
- **NON** toccare `Lexend-Bold.ttf` e `Lexend-ExtraBold.ttf`: se tornano a 14 byte sono corrotti, riestrarli da `Lexend-var.ttf`.
- **MAI** scrivere API key o segreti dentro file di memoria o del repo: stanno solo in `~/.secrets/adoff-stores.env`. La vecchia chiave Edge era in chiaro in un file di memoria ed è stata rimossa.

## Key Decisions

- Sostituzione integrale di `convertPattern()` con modulo `abp-urlfilter.js` come **unica porta** di conversione pattern → DNR, per evitare drift tra feed e validatore.
- Quota curate/live fissata a **60/40**, ALLOW sempre completo (anti falso positivo), BULK escluso.
- `build-rules-feed.js` con **fail-fast al 5%** di `urlFilter` invalidi: nessun feed invalido raggiunge il deploy.
- Doppio gate per regola in `parse-adblock-to-dnr.js`: validazione + ancoraggio al dominio, blocco sotto 50% di ancorate.
- Il gate i18n era già hard-blocking e non è stato toccato: a essere incompleti erano i **dati**. Sanate 29 chiavi assenti dalla matrice, riempite 1.139 celle, eliminate 7 chiavi `about.*` morte. Nessuna soglia allentata.
- Le 7 chiavi `about.*` sono state **rimosse** invece che tradotte: zero riferimenti in tutto `site/` e valori corrotti in matrice (`">About me"`, identici in ogni lingua, privi di `it`/`en`).
- Edge API key **aggiornata** in `~/.secrets/adoff-stores.env` (dove già viveva); nessun segreto nel repo né in file di memoria.

## Current State

- Release **3.5.59** live su Chrome Web Store, Firefox AMO, Microsoft Edge Add-ons.
- Safari: da pubblicare, vincolata a toolchain Apple.
- Feed remoto `2026.08.02.2` attivo: `adoffRemoteRulesCount == 29000`, caricamento in 3,0 s, blocco confermato su `0019x.com`.
- i18n: 0 hard failures; 1.561 celle soft untranslated residue (616 IT).
- `feat/premium-vpn` HEAD `06dd73e` pushato, 8 commit cumulativi.

## File Changes

- Nuovi:
  - `sviluppo/filter-lists/abp-urlfilter.js`
  - `sviluppo/filter-lists/rule-priority.js`
  - `sviluppo/filter-lists/build-rules-feed.js`
  - `sviluppo/tests/verify-generated-feed.js`
  - `sviluppo/tests/canary-feed-falsepositives.js`
  - `sviluppo/scripts/i18n_collect_gaps.py`
- Riscritti:
  - `sviluppo/filter-lists/parse-adblock-to-dnr.js`
- Dati i18n (lo script `deploy-site.sh` NON è stato toccato: era il contenuto a essere incompleto):
  - `site/i18n/_matrix.json` (+1.168 celle, −7 chiavi morte)
  - `site/i18n/*.json` (15 dizionari runtime rigenerati)
  - `site/**/*.html` (43 pagine `vs/`: critical CSS inline rigenerato dal deploy)
- Output pubblicato:
  - `site/rules-feed.json` (feed `2026.08.02.2`)
  - `sviluppo/archive/rules-feed-pre-generatore-20260802.json` (backup del feed precedente)
- Documentazione:
  - `.claude/TODO.md`
- Esterni (fuori dal repo):
  - `~/.secrets/adoff-stores.env` (Edge API key aggiornata, backup datato a fianco)
  - `~/Obsidian/Memoria/progetti/chromeplugin/sessioni/sessione-20260802-generatore-feed-bonificato.md`

## Code Context

**Firma gate di validazione** (verbatim dal brief):
`isValidUrlFilter` rifiuta parentesi, backslash e schemi `http:`.

**Forma dominio canonica**: `||dominio^`

**Quote rule-priority.js**:
- ALLOW: tutte (2/2 effettive)
- CURATED: 60% del budget (17.399/25.737)
- LIVE: 40% del budget (11.599/28.957)
- BULK: 0/77.604

**Cap dinamico runtime**: `MAX_NUMBER_OF_DYNAMIC_RULES == 30000` (NON usare `MAX_NUMBER_OF_DYNAMIC_AND_SESSION_RULES`).

**Indicatori end-to-end**:
- storage: `adoffRemoteRulesCount == 29000`
- tempo applicazione: 3,0 s
- blocco test: `0019x.com` → regola `id 177` del candidato
- regole statiche filtrate da `matchedRules` con `rulesetId == "_dynamic"`
- chunking applicazione: blocchi da 2000

**Soglie pipeline**:
- `parse-adblock-to-dnr.js`: blocco output se < 50% pattern ancorati al dominio
- `build-rules-feed.js`: rifiuto scrittura se > 5% `urlFilter` invalidi
- i18n `deploy-site.sh`: hard-fail a 0 failures

**Edge API**: auth test valido solo via POST (es. creazione submission `1152921505701564400`). GET su `/v1/products/ID/submissions` non esiste.

## Setup

- Eseguire con `NODE_PATH` puntato a `sviluppo/reviews/node_modules`.
- SEMPRE sotto `xvfb-run -a`. **Mai** `channel: chrome` in Playwright: non inietta il world MAIN.
- Per testare la build offuscata: variabile `EXT_PATH` su `sviluppo/build-chrome`.
- Per isolare il service worker dal feed live: argomento Chrome `host-resolver-rules` che mappa `adoff.app` → `127.0.0.1`.

Comandi (righe indentate, non in code fence):

  `node sviluppo/filter-lists/parse-adblock-to-dnr.js`

  `node sviluppo/filter-lists/build-rules-feed.js --out <path> --live <path feed attuale>`

  `NODE_PATH=sviluppo/reviews/node_modules FEED_PATH=<path> xvfb-run -a node sviluppo/tests/verify-generated-feed.js`

  `bash sviluppo/scripts/deploy-site.sh`

## Resume Instructions

1. Riprendere dal To-Do #1: pubblicare **Safari 3.5.59**. Servono Mac, Xcode, `xcrun safari-web-extension-converter`. Convertire la build MV3 corrente, firmare con certificato sviluppatore Apple, caricare su App Store Connect.
2. Safari non dipende dal feed: il pacchetto è già pronto in `site/adoff-safari.zip` (generato da `node sviluppo/scripts/build.js --target safari`). Rieseguire `verify-generated-feed.js` serve solo se si rigenera il feed.
3. Successivamente To-Do #2: ridurre le **1.561 celle untranslated** (focus 616 IT) usando `sviluppo/scripts/i18n_collect_gaps.py` + traduzione a blocchi da 25.
4. Poi To-Do #3: valutare sperimentalmente lo spostamento della quota **60/40** curate/live sulle 103.300 regole oggi escluse.
5. In ogni test, mantenere `host-resolver-rules` `adoff.app → 127.0.0.1` per evitare contaminazione dal feed live.
6. Per autenticarsi su Edge in test: usare **POST** (es. creazione submission), mai `GET /v1/products/ID/submissions`.
7. Nessun segreto nel repo né in file di memoria: tutto in `~/.secrets/adoff-stores.env`.
8. Branch: restare su `feat/premium-vpn` (HEAD `06dd73e`); `main` intoccato (open-core pubblico).

## Warnings

- Il feed live `adoff.app` maschera sempre i match del candidato se la rete non è isolata: ogni `verify-generated-feed.js` senza `host-resolver-rules` produce risultati falsamente positivi.
- `testMatchOutcome` espone `matchedRules` (NON `matchResults[0]`); senza filtro `rulesetId == "_dynamic"` gli id statici vengono scambiati per regole del feed.
- `updateDynamicRules` è atomica: passare l'intero feed in un colpo solo fallisce in blocco senza dettaglio. Procedere a chunk da 2000.
- `MAX_NUMBER_OF_DYNAMIC_AND_SESSION_RULES == 5000` è la trappola classica: usare `MAX_NUMBER_OF_DYNAMIC_RULES == 30000`, letto a runtime.
- Confronto feed vecchio/nuovo: canonizzare la chiave **togliendo** il prefisso `||` e il `^` finale prima del diff (il feed storico scrive `||dominio`, il generatore `||dominio^`), altrimenti la continuità crolla da 28.957 a 5.
- Traduzione massiva: blocchi da 25, validazione e retry; blocchi grandi producono JSON malformato.
- `deploy-site.sh` è l'unica via di rilascio del sito: `wrangler pages deploy` diretto bypassa il gate i18n e pubblica 774 hard failures.
- `feat/premium-vpn` NON va mergeato su `main`: `main` è il repo pubblico open-core.
- Font: `Lexend-Bold.ttf` e `Lexend-ExtraBold.ttf` se tornano a **14 byte** sono corrotti: riestrarli da `Lexend-var.ttf`.
- Segreti Edge: la vecchia chiave era in chiaro in un file di memoria già rimosso. Verificare che nessun dump corrente la contenga prima di persistere checkpoint.
